import './assets/main.css' // appel mon css
import { createApp } from 'vue' // appel ma fonction createApp de vueJS
import App from './App.vue' // appel App 

createApp(App).mount('#app') // appel le app construit dans App.vue et ceci je le monte pour l'appliqué à id #app
